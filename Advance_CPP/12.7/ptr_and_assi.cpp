#include<bits/stdc++.h>
using namespace std;

int main(){
    int x{5};
    int* ptr{&x};// pointer initialized to point at x ;
    cout<<*ptr<<endl;// prints the value at the add being pointer to (x's address )
    int y 

}